---
import { wallpaperItems } from "../data/wallpaper";

const critterItems = wallpaperItems.filter((item) => item.category === "Critters");
---

<main class="py-8">
  <div class="max-w-3xl">
    <h1 class="text-4xl font-bold tracking-tight text-slate-900">
      Critters
    </h1>
    <p class="mt-3 text-lg leading-8 text-slate-600">
      Browse Wahkonsa Lodge critter wallpaper images.
    </p>
  </div>

  <div class="mt-6 grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-4">
    {
      critterItems.map((item) => (
        <a
          href={item.image}
          target="_blank"
          rel="noopener"
          class="group rounded-lg border border-slate-200 bg-white p-3 shadow-sm transition hover:-translate-y-0.5 hover:border-slate-300 hover:shadow-md focus:outline-none focus-visible:ring-2 focus-visible:ring-teal-600 focus-visible:ring-offset-2"
          aria-label={`View ${item.title}`}
        >
          <div class="aspect-[4/3] overflow-hidden rounded-md bg-slate-50">
            <img
              src={item.thumb}
              alt={item.title}
              loading="lazy"
              class="h-full w-full object-cover transition duration-200 group-hover:scale-[1.02]"
            />
          </div>
          <p class="mt-3 truncate text-sm font-medium text-slate-700">
            {item.title}
          </p>
        </a>
      ))
    }
  </div>
</main>
